syntax = "proto3";

package twirpapp.rpc;
option go_package = "/rpc";

message LongUrl {
  string url = 1;
}

message ShortUrl {
  string url = 1;
}

service UrlShortner {
  rpc Shorten(LongUrl) returns (ShortUrl);
}
